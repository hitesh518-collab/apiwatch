openapi: 3.0.3
info:
  title: D-07 positional path template identity
  version: '1'
paths:
  /users/{userId}/orders/{orderId}:
    get:
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
        - name: orderId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: ok
