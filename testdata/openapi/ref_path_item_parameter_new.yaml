openapi: 3.0.3
info:
  title: Example
  version: 1.0.0
paths:
  /users:
    $ref: "#/paths/~1user-template"
  /user-template:
    get:
      parameters:
        - name: limit
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
