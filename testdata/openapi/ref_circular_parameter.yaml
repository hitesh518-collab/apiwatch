openapi: 3.0.3
info:
  title: Example
  version: 1.0.0
components:
  parameters:
    Limit:
      $ref: "#/components/parameters/Limit"
paths:
  /users:
    get:
      parameters:
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
