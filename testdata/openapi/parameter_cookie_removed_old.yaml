openapi: 3.0.3
info:
  title: Example
  version: 1.0.0
paths:
  /users:
    get:
      parameters:
        - name: session
          in: cookie
          schema:
            type: string
      responses:
        "200":
          description: OK
