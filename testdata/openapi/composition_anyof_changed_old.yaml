openapi: 3.0.3
info:
  title: Example
  version: 1.0.0
paths:
  /search:
    get:
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                anyOf:
                  - type: object
                    properties:
                      result:
                        type: string
                  - type: string
